# Web UI backend. Install alongside the project itself:
#   pip install -e ".[dev]"            (from the repo root)
#   pip install -r web/backend/requirements.txt
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
